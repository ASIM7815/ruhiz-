'use client';

import AppShell from '@/components/app/AppShell';

/**
 * Ruhiz app shell — mounts the full social experience:
 * Home / Explore / My Journey / Connections / Messages /
 * Notifications / Saved / Profile / Settings, with the
 * Photo / Video / Moment creation flows on top.
 */
export default function RuhizFeed() {
  return <AppShell />;
}
